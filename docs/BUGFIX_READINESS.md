# Bug 修复就绪清单

更新时间：2026-08-15

检查基线：`be011cf88e3ee76798e0cfc6c6ba9c89599bc975`（`main`）

状态：**文档交接已具备，产品实现基线尚未落地，暂不具备复现或修复具体 Bug 的条件。**

## 1. 仓库确认

- Git 顶层目录：`D:\deepseek-agent\dsh-research-cat-platform`。
- 当前分支：`main`。
- 检查时工作区：干净。
- 最近提交：`be011cf docs: define research platform and bugfix handoff`。
- 当前未配置 Git remote；因此这里只能确认本地 canonical 工作目录，不能核验远端仓库归属或同步状态。
- 唯一允许写入的项目目录是本仓库。只读参考源不得修改，也不得从中读取、复制或提交 `.env`、令牌、数据库、备份、上传文件或真实用户数据。

## 2. 已具备的交接信息

- `README.md`：仓库用途、当前阶段、必读文档与只读参考源。
- `docs/PRODUCT_CHARTER.md`：目标用户、三入口信息架构、科研绘图最小闭环、质量底线与阶段路线。
- `docs/ARCHITECTURE_GUARDRAILS.md`：DSH 插件/桌面壳/UI/状态边界，以及禁止覆盖 single slots、禁止 DOM 猴子补丁等约束。
- `docs/BUGFIX_PLAYBOOK.md`：基线确认、稳定复现、失败证明、根因分析、最小修复、分层验证与交付记录流程。
- `docs/BUG_REPORT_TEMPLATE.md`：环境、复现、证据、安全影响与验收字段。
- 已明确插件失败必须安全降级，不能让 Harness 停留在 `Loading plugins...`。
- 已明确没有稳定复现或验证证据时不得宣称“已修复”。

## 3. 实现代码仍缺少的信息

当前仓库只有 Markdown 文档，没有实现代码或工程清单。以下内容必须由产品实现基线或后续交接提供；在此之前不猜测命令：

- 应用与各包的目录结构、入口点和模块所有权。
- 包管理器及锁文件、Node/pnpm/dsh 的受支持版本。
- 开发启动、桌面启动、生产构建和安装包构建命令。
- 单元、契约、集成、端到端与 UI 测试的命令和测试数据策略。
- 类型检查、lint、格式检查和 Markdown 检查的实际工具与命令。
- DSH host/browser 插件清单、加载顺序、超时/错误边界及卸载行为。
- 环境变量的名称与安全配置方式（只记录名称和用途，不记录值）。
- 本地端口、单实例、子进程、日志位置和清理策略。
- FigureProject、FigureBrief、GenerationJob、FigureVersion 的持久化与迁移方式。
- mock 生成、mock 计费、失败注入和幂等验证方法。
- 支持的 Windows 版本、窗口尺寸、缩放、主题与减少动态效果矩阵。
- Browser 验证所使用的浏览器、固定视口、基线截图和允许差异。
- CI 门禁、发布流程、版本来源与回滚步骤。
- remote/默认分支/受保护分支等远端仓库信息。

## 4. 每个 Bug 必须提供的证据

提交 Bug 时使用 `docs/BUG_REPORT_TEMPLATE.md`，至少填写：

1. 当前应用版本或完整 commit，以及复现时工作区是否有未提交改动。
2. Windows、Node、pnpm、dsh、应用和相关插件版本。
3. 实际使用的安装/启动命令；不得包含密钥或其他秘密值。
4. 能稳定触发问题的最短步骤，以及问题发生前所需状态。
5. 期望结果、实际结果、发生频率和首次/最近发生时间。
6. 完整错误文本或安全脱敏后的日志文件路径，并标记关键时间点。
7. 截图或录屏；UI Bug 还需浏览器、视口、缩放、主题、减少动态效果和插件状态。
8. 最近正常版本、可疑变更或已排除项（如已知）。
9. 数据丢失、重复生成/扣费、密钥或个人信息等安全影响。
10. 可接受的验收标准，以及是否允许在指定浏览器中进行 UI 验证。

证据不得包含 `.env` 内容、令牌、数据库、备份、上传文件或真实用户数据。需要日志时，应先脱敏并只提供与缺陷相关的最小片段或安全路径。

## 5. 修复任务的证据链

每次收到具体 Bug 后，按 `docs/BUGFIX_PLAYBOOK.md` 保存并报告以下证据：

- **基线证据**：commit、分支、工作区状态、环境版本与实际启动命令。
- **失败证据**：修复前可重复失败的测试，或同一视口下的 UI 复现、日志与明确断言。
- **根因证据**：触发条件、错误机制、责任层和影响范围，不以最后一行报错或单张截图代替分析。
- **补丁证据**：只包含根因层的最小改动，并保留现有用户改动。
- **验证证据**：相关测试、类型检查、构建、启动、主路径与失败路径的实际命令和结果。
- **UI 证据**：适用时使用用户选择的 Browser 能力，在相同视口、缩放、主题和状态下比较修复前后；不得只凭截图猜 CSS。
- **交付证据**：改动文件、剩余风险、未覆盖项和可执行的回滚方式。

## 6. 当前可执行范围

在实现基线和具体 Bug 到达前，只进行只读诊断与质量就绪检查，不修改功能实现。当前等待：

- 产品 AI 的视觉选择与实现基线；或
- 一份按模板填写、且不包含秘密或真实用户数据的具体 Bug 报告。
